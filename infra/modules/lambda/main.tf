/*
  Milestone 1: casca (sem criar Lambda ainda) para permitir init/validate.
  Milestone 3: criar aws_lambda_function + aws_cloudwatch_log_group.
*/

# Placeholder para evoluir sem quebrar estrutura
locals {
  placeholder = "lambda-module-skeleton"
}
